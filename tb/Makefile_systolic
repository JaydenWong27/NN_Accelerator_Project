export PATH := $(CURDIR)/.venv/bin:$(PATH)

SIM = icarus
TOPLEVEL_LANG = verilog
VERILOG_SOURCES = $(CURDIR)/../rtl/pe.sv $(CURDIR)/../rtl/systolic_array.sv
TOPLEVEL = systolic_array
MODULE = systolic_array_pe

COCOTB_ROOT := $(shell $(CURDIR)/.venv/bin/python3 -c "import cocotb_tools, os; print(os.path.dirname(cocotb_tools.__file__))")
include $(COCOTB_ROOT)/makefiles/Makefile.sim
